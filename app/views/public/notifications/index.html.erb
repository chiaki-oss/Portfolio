<h2>あなたへのお知らせ</h2>
<div>
	<% notifications = @notifications.where.not(visitor_id: current_user.id) %>
	<% if notifications.exists? %>
        <!-- collectionオプション 複数形で渡す＝そのテンプレートを繰り返し表示できる -->
		<%= render notifications %>

	<% else %>
		<P>通知はありません</P>
	<% end %>

	<p><%= link_to '戻る', :back, class: "btn btn-default" %></p>

</div>