<h1>投稿一覧</h1>
<div class="row">
	<% @posts.each do |post| %>
		<%= link_to post_path(post) do %>
			<%= attachment_image_tag post, :image, format: "jpeg", fallback: "no-image.jpg" %>
			<p><%= post.title %></p>
		<% end %>
	<% end %>
</div>