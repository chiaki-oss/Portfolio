<!-- SIDEBAR -->
<div>
	<%= render 'layouts/sidebar', {genres: @s_genres, tags: @s_tags, areas: @s_areas} %>
</div>

<h2>タイムライン
<% if @following_posts.count > 0 %>
 <%= link_to 'Following User', user_follows_path, class: "btn btn-default" %>
</h2>
<% else %>
    <p>フォローしているユーザーがいません</p>
<% end %>
<%= render 'public/posts/index', posts: @following_posts, post: @post %>